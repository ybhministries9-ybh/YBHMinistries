/* SUPABASE DISABLED - Using static fallback data only */

// Disabled Supabase to prevent errors
// The admin components will now use only static/fallback data
export const projectId = ""
export const publicAnonKey = ""